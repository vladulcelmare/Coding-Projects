# Coding Projects

> A collection of personal projects built during my free time, focused on **numerical methods**, **low-level implementations**, **data structures**, and **machine learning fundamentals**.

---

## Numerical Methods

### LU Decomposition (`LU.py`)
- Performs **LU factorization** for square matrices 
- Useful for solving linear systems efficiently

---

### QR Decomposition (`QR.py`)
- Performs **QR factorization** for square matrices
- Applicable in least squares and eigenvalue algorithms

---

## Applications

### Total Commander Clone (`TotalCommander.py`)
A simplified version of the classic file manager, featuring:
- File navigation
- Directory management
- Basic file operations
- Encryption

---

## Machine Learning

### Trigram Language Model + Neural Network (`trigram_counting+nn.py`)
- Character-level **trigram model**
- Two approaches:
  -> Frequency-based counting
  -> Simple neural network

---

### Custom MLP from Scratch (`newnn.py`)
A fully manual implementation of a **Multi-Layer Perceptron**, inspired by how frameworks like PyTorch work internally.

**Includes:**
- Forward propagation
- Backpropagation
- Gradient descent optimization

---

## Data Structures (C++)

### Template Tree Implementation (C++ OOP)
A custom implementation of a **generic (templated) tree data structure** in C++ using Object-Oriented Programming principles.

**Features:**
- Generic node structure using templates
- Support for hierarchical tree representation
- Recursive traversal operations  
- Designed for extensibility and reuse in different contexts

---

## Future Improvements

- Create new repositories with more advanced projects
- Extend LU / QR to **non-square matrices**  
- Explore advanced architectures:
  -> RNNs
  -> Transformers
- Expand C++ structures from scratch with:
  -> Balancing trees
  -> STL from scratch   
  -> Smart pointers  

--- 

`Python` • `C++` • `OOP` • `Templates` • `Linear Algebra` • `Machine Learning`
